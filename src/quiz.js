/**
 * Given a particular quiz, permanently remove the quiz.
 * 
 * @param {int} authUserId - a user's unique identification number
 * @param {int} quizId - a quiz's unique identification number
 * @returns {} - an empty object
 */

function adminQuizRemove(authUserId, quizId) {
    return {}
}